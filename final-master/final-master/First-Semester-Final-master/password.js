class Password{
  //The first part of the class.
constructor(pb,pv){
  this.publickey = pb;
  this.privatekey = pv;
}

  //Instance functions below this comment.
validPublickey(){
  if(this.publickey.length>=8 && this.publickey.length<=25){
    return true;
  }
   else{}
    return false;
  }
}

validPrivatekey(){
  ifthis.privatekey.chart([4])&&[9 == [;

  }

}]
